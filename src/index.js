import React from 'react';
import ReactDOM from 'react-dom';
import './components/Style.css'; // Ensure this path matches the location of your CSS file
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
